# Local configuration
base.url=https://penza.hh.ru
browser=chrome
browser.version=127.0
browser.size=1920x1080

# Local browser (no Selenoid)
use.local.browser=true

# Timeouts
page.load.strategy=eager
timeout=15000

# Reports
reports.folder=build/reports